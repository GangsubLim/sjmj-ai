# sjmj-ai Context

수기 거래명세서 OCR 자동입력 플랫폼. PHP+React 원본(SJMJ-Web)을 Python(FastAPI)+React로 동형 포팅해 macmini에 배포(Phase 1)하고, 검증된 ML PoC를 이음새에 끼워 production 추론 + 피드백 루프로 승격(Phase 2)한다.

## Language

### ML 도메인 (Phase 2)

**품목 인식기 (Item recognizer)**:
사진의 품목칸을 작성자-특화 retrieval로 식별하는 인식기. `ddobokki/ko-trocr` ViT 인코더 + contrastive head(torch). 재학습 대상이며, 뱅크 대조로 top-5 후보를 낸다.
_Avoid_: 그냥 "모델"(어느 모델인지 모호 — 금액 인식기와 구분 필수)

**품목 크롭 (Item crop)**:
품목 인식기에 넣기 전, warp된 전표에서 품목칸 영역을 잘라내는 전처리 단계(`ITEM_X` 고정 x좌표 + 잉크 기반 우측 동적 확장). 학습 가능한 파라미터가 없는 순수 기하학적 처리이며, 재학습(품목 인식기 fine-tune) 대상이 아니다.
_Avoid_: "크롭 엔진을 학습시킨다"(크롭 단계엔 학습 파라미터가 없음 — 재학습은 크롭 이후의 품목 인식기만 해당. 근거: ADR 0005)

**품목칸 잉크 (Item cell ink)**:
가로줄로 나눈 칸 전체에서 손글씨 획이 차지하는 비율. **이 행에 새 품목이 적혀 있는가**를 판정하는 데 쓰이며, 행검출 정확도를 높이려면 이 기준선을 낮추는 방향으로 움직인다.
_Avoid_: 그냥 "잉크"(어느 영역을 재는지 모호 — 크롭 잉크율과 구분 필수)

**크롭 잉크율 (Crop ink ratio)**:
저장된 품목 크롭 안에서 손글씨 획이 차지하는 비율. 크롭 높이는 대개 행 밴드 전체(≈82px)이고, 글씨가 있는 행과 빈 행의 값이 크게 벌어진다. 드물게 스냅이 실제로 작동한 짧은 크롭은 분모가 작아 잉크율이 오히려 높게 나오므로, **옅은 획이 새 품목으로 오승격된 갈래의 빈 크롭은 이 축으로 잡히지 않는다.** 품목칸 잉크와 **의도적으로 다른 축**이다 — 품목행 판정과 빈 크롭 판정이 서로를 되돌리지 않게 하려는 것이므로, 두 기준선은 독립으로 조정한다.
_Avoid_: 그냥 "잉크", "품목칸 잉크"(같은 값으로 오해 — 재는 영역이 다르다. 근거: ADR 0006)

**약식 분해 (Split entry)**:
한 품목의 수량·단가가 여러 줄에 걸쳐 적히고 품목명은 첫 줄에만 쓰는 필기 관행. 아래 줄들은 품목칸을 비운 채 금액칸만 채우며, 그 금액은 위 품목에 합산된다 — DB에는 합산값 한 건, 손글씨에는 세부 내역이 남는다. 손글씨 이미지 자체에 이 규칙이 적혀 있지 않아 인식이 스스로 알아낼 수 없고, 행 판정 규칙으로 흡수한다.
_Avoid_: "여러 행 품목", "분할 입력"

**연속행 (Continuation row)**:
약식 분해에서 품목칸이 비고 금액칸만 채워진 줄. 품목이 아니므로 **크롭도 학습 후보 쌍도 만들지 않고** 금액만 위 품목에 합산된다. 정상 전표에서 흔하다.
_Avoid_: "빈 행"(금액칸까지 빈 줄과 혼동), "빈 크롭"(연속행은 애초에 크롭되지 않는다)

**블록 (Block)**:
한 품목을 이루는 줄 묶음 — 새 품목 줄 하나 + 뒤따르는 연속행들. 확정 후 거래명세서 항목 한 건에 대응한다. 새 품목 줄이 없는 채 연속행으로 시작한 블록은 이상신호다.

**빈 크롭 (Blank crop)**:
품목 크롭까지 만들어졌는데 그 그림에 손글씨 획이 사실상 없는 행. 라벨은 붙어 있으나 그림에 근거가 없어 학습 정답지로 성립하지 않는다. **연속행과 다르다** — 연속행은 품목칸이 비어도 애초에 크롭되지 않으므로 여기 해당하지 않는다. 빈 크롭은 두 갈래로 생긴다: **행 오프셋**의 꼬리(밀려난 마지막 줄이 빈칸을 가리키게 됨), 또는 연속행의 옅은 획(〃·대시·삐침)이 품목칸 잉크 기준선을 넘어 **새 품목으로 잘못 승격**된 경우.

**크롭 좌표 (Crop ref)**:
품목 크롭 하나를 가리키는 `job-42/row-3` 형식의 참조. 이름이 아니라 **좌표**다 — 뒤의 번호는 그 잡에서 검출된 새 품목 줄의 몇 번째인지를 뜻하므로, 행검출이 달라지면 같은 문자열이 다른 줄을 가리킨다. 학습 후보 쌍은 이 좌표를 유일키로 쓰기 때문에, 좌표의 의미가 바뀌는 순간(재처리) 라벨과 그림의 대응이 조용히 어긋난다.
_Avoid_: "크롭 식별자", "크롭 ID"(잡이 바뀌어도 같은 것을 가리키는 안정적 이름으로 오해)

**행 오프셋 (Row offset)**:
행검출이 실제 명세서보다 한 칸 이상 밀려, 크롭 그림과 확정 라벨이 서로 다른 행을 가리키게 된 상태. 검수자는 원본 순서대로 입력하므로 이 어긋남은 검수 화면에서 드러나지 않고 그대로 확정된다. **빈 크롭 가드로는 탐지되지 않는다** — 밀렸어도 그림에 글씨가 있으면 잉크로 구분할 수 없다. 오프셋 자체를 줄이는 일은 행검출 정확도의 몫이고, 빈 크롭 가드는 그 잔여 오검출이 학습 데이터로 번지는 것만 막는다.
_Avoid_: "크롭 불량"(크롭 경계가 잘린 것과 행이 밀린 것은 원인·대책이 다르다)

**금액 인식기 (Amount recognizer)**:
금액칸 손글씨를 전사하는 VLM. `Qwen3-VL-8B-Instruct-4bit`(MLX). **추론 전용 — 절대 학습하지 않는다.** 어휘 무관이라 신규 전표에도 즉시 작동.
_Avoid_: 그냥 "모델", "VLM 학습"(금액 인식기는 학습 대상이 아님)

**재학습 (Retraining)**:
품목 인식기의 마지막 2개 ViT 층 + head만 fine-tune하고 `bank.npz`를 재생성하는 분 단위 잡(`train_contrastive --production`, 22 epoch). 8B 금액 인식기는 건드리지 않는다.
_Avoid_: "모델 학습"(8B를 학습하는 것으로 오해), "트레이닝"

**뱅크 (Bank)**:
품목 인식기가 retrieval하는 임베딩 사전(`bank.npz`). 교정 crop이 누적될수록 갱신되어 신규→재현 어휘 전환으로 정확도가 오른다. **뱅크는 크롭에서만 자란다** — 항목의 실체가 그림의 임베딩이라, 그림이 없는 이름은 구조적으로 항목이 될 수 없다. 자동완성 사전에 이름을 추가해도 뱅크는 변하지 않으며, 그 품목이 실제 전표에 나타나 크롭이 생겨야 비로소 들어온다.

**정식 라벨 (Canonical label)**:
학습에 쓰는 정규화된 품목 이름. 청구 이름(거래명세서에 적힌 자유 텍스트)과 **의도적으로 갈라질 수 있다** — 청구는 사실의 기록이고 정식 라벨은 학습 정답지라 정규화 기준이 다르다(근거: ADR 0004). 뱅크에 실리는 것은 이것이며, 따라서 정식 라벨의 집합이 곧 **품목 인식기가 추천할 수 있는 어휘의 전부**다.
_Avoid_: "품목명"(청구 이름·자동완성 이름과 뭉뚱그려진다)

**자동완성 사전 (Item suggestion list)**:
사람이 거래명세서를 입력할 때 고르는 품목 이름 목록. **편의 목록이지 품목 마스터가 아니다** — 여기 없는 이름도 자유롭게 입력·저장되고, 참조 무결성의 원천이 아니다. 정식 라벨과 층위가 다르지만 사람이 검색하는 어휘와 인식기가 추천하는 어휘가 갈리면 같은 물건이 두 이름으로 학습쌍을 쪼개므로, 정식 라벨 → 자동완성 사전 **한 방향으로만** 잇는다(근거: ADR 0008).
_Avoid_: "품목 DB", "품목 마스터"(참조 무결성의 원천으로 오해)

**매칭키 (Matching key)**:
기존 운영 invoice ↔ 스캔 사진을 사후 짝짓는 **backfill 전용** 키 `invoices.total_supply`(공급가합, VAT 제외). 초기 학습쌍 부트스트랩에만 쓴다. 라이브 HITL 플로우는 매칭키를 안 쓰고 job↔invoice를 직접 연결한다. `grand_total`(VAT 포함)로 바꾸면 조회 0건.
_Avoid_: grand_total, 합계금액

**추론 잡 (OCR job)**:
사진 업로드부터 초안 JSON까지의 비동기 작업 단위. `ocr_jobs` 테이블에 상태(pending/running/done/failed)로 추적.

**강등 (Demotion)**:
워프 정합 게이트가 오검출 워프를 판정해 초안을 비운 채 잡을 done으로 끝내는 일. 쓰레기 초안과 학습쌍 크롭이 만들어지는 것을 막으려는 **의도된 결말**이라 예외로 죽은 실패(`failed`)와 원인도 대책도 다르다 — 실패는 다시 돌리면 될 수도 있지만, 강등 잡은 같은 사진이면 같은 결말이고 사람이 볼 것은 왜 정합에 실패했는가다. 쿼드 미검출도 같은 결말(빈 초안)로 빠지지만 원인이 달라 갈라 센다.
_Avoid_: "실패"(잡 상태 `failed`와 혼동 — 큐레이션 분석이 세는 **검수 대상 실패**와도 다르다), "빈 결과"(원인이 지워진다)

**게이트 지표 (Gate metrics)**:
강등 판정의 근거 수치(가로줄 수·행간 편차·청색 비율·청색 비대칭). 판정과 **의도적으로 다른 축**이다 — 판정은 통과/강등 한 비트뿐이라 임계를 재조정하고 나면 과거 잡을 그 시점 기준으로 되읽을 수 없고, 지표가 남아야 캘리브레이션 변경 전후를 같은 자로 잰다.
_Avoid_: "warp_ok"(판정 비트일 뿐 근거가 아니다)

**단계 기하 (Stage geometry)**:
한 잡이 각 단계에서 실제로 쓴 좌표·수치 묶음 — 쿼드 4점과 그것을 낸 공급자, deskew 각도, 가로줄과 피치, 행 밴드와 그 분류, 품목·금액 크롭 박스. 판정이 아니라 **판정의 재료**이며, 어느 단계가 틀어졌는지 사람이 눈으로 가리려면 이것이 남아 있어야 한다. 그 잡이 **실제로 겪은 것**이라 지금 엔진으로 다시 계산해 만들 수 없다 — 재계산은 관측이 아니라 다른 실행이다. 크롭·워프 이미지와 같은 추론 산출물이라 같은 자리에 살고, 없으면 그 잡은 조용히 관측 없음으로 닫힌다. 게이트 지표와 층위가 다르다 — 그쪽은 강등 판정의 근거 4종뿐이다.
_Avoid_: "디버그 정보"(무엇이 남는지 모호), "게이트 지표"(강등 판정의 근거만 가리킨다)

**재처리 (Reprocessing)**:
이미 처리가 끝난 추론 잡을 현재 엔진으로 다시 판정하는 일. 확정된 거래명세서는 건드리지 않고 크롭·초안과 학습 후보 쌍의 좌표만 갱신한다. 확정 라벨은 사람이 내린 사실이라 남고 그 라벨이 가리키던 그림이 바뀌므로, 재처리의 본질은 크롭을 새로 만드는 것이 아니라 **라벨과 그림의 대응을 다시 맺는 것**이다. 엔진 개선분을 과거 데이터에 미치게 하는 유일한 경로이며, 대응을 다시 맺지 않은 채 크롭만 갈아끼우면 스스로 행 오프셋을 만들어낸다.
_Avoid_: "재큐잉"(줄을 다시 세우는 일로만 읽혀 대응 재수립이 빠진다), "재추론"

**행 앵커 (Row anchor)**:
재처리 전후로 같은 줄을 알아보게 하는 표식 — 사람이 확정한 공급가를 쓴다. 순번은 행검출이 달라지면 밀리고 품목명은 재처리로 고치려는 대상이라 둘 다 표식이 될 수 없는 반면, 확정 공급가는 사람이 눈으로 보고 입력한 값이라 줄마다 다르고 엔진 변화에 영향받지 않는다. 전표를 사후 짝짓는 매칭키와 같은 발상의 **행 단위** 판이다. 앵커로 짝지어지지 않은 줄은 라벨을 승계하지 않고 사람에게 넘긴다 — 애매한 승계보다 드러난 미결이 낫다.
_Avoid_: "매칭키"(전표 단위 backfill 키와 혼동 — 층위가 다르다)

**승계 (Relink)**:
재처리로 새로 만들어진 크롭에 기존 확정 라벨을 다시 붙이는 일. 행 앵커로 짝지어진 줄에만 적용하며, 라벨은 그대로 두고 그 라벨이 가리키는 좌표를 옮긴다. 재처리의 목적이 여기 있다 — 크롭을 새로 만드는 것은 승계를 위한 준비일 뿐이고, 승계 없이 크롭만 갈아끼우면 행 오프셋을 스스로 제조한다(근거: ADR 0007).
_Avoid_: "재매핑"(무엇이 무엇으로 옮겨가는지 사라진다), "라벨 이전"(라벨이 움직인다는 오해 — 움직이는 것은 좌표다)

**미결 쌍 (Orphaned pair)**:
승계에 실패해 확정 라벨은 남았으나 가리킬 그림을 잃은 학습 후보 쌍. 라벨은 사람이 눈으로 보고 내린 사실이라 지우지 않고, 배제 사유를 달아 학습에서만 뺀다. **빈 크롭과 다르다** — 빈 크롭은 그림이 있는데 획이 없는 것이고, 미결 쌍은 그림 자체가 없다. 애매한 승계보다 드러난 미결이 낫다는 판단의 산물이므로, 이 수가 곧 재처리가 치른 값이다.
_Avoid_: "빈 크롭"(그림 유무가 다르다), "실패한 쌍"(라벨은 여전히 유효한 사실이다)

**초안 (Draft)**:
추론 잡이 낸 미확정 결과 JSON(품목 top-5 + 금액). 사람이 검수·교정하기 전 상태.

**미확정 잡 (Unconfirmed job)**:
초안까지 나왔는데 사람이 확정하지 않아 거래명세서도 학습 후보 쌍도 만들지 못한 잡. 기계는 할 일을 다 했으므로 **파이프라인 신호가 아니라 사람 쪽 누수**이며, 잡 상태는 성공한 잡과 똑같은 done이라 거래명세서와 이어졌는지로만 갈린다. 강등 잡과 함께 큐레이션에 영영 닿지 않는 두 갈래를 이룬다.
_Avoid_: "실패한 잡"(기계는 성공했다), "대기 잡"(추론을 기다리는 pending과 혼동)

**교정 (Correction)**:
사람이 초안을 고친 내용(`ocr_corrections`). 곧 재학습의 정답지가 된다.

**학습 후보 쌍 (Training pair)**:
confirm된 잡의 각 행에서 나온 `(row crop 이미지 → 확정 품목 라벨)` 쌍. 사용자가 라벨을 고쳤는지와 무관하게 확정된 모든 행이 후보다(맞춘 행은 양성 라벨). 품목 인식기 재학습의 단위이며, 금액은 학습 대상이 아니라 후보를 이루지 않는다.
_Avoid_: "수정쌍"(변경된 행만으로 오해), "학습 데이터"(범위 모호)

**부트스트랩 세트 (Bootstrap set)**:
품목 인식기를 처음 학습시키고 최초 뱅크를 만든 전표 사진 묶음. 라이브 HITL 이전에 기존 운영 invoice와 사후 매칭(매칭키)해 모은 것이라, 학습 후보 쌍과 달리 큐레이션을 거치지 않았고 `training_pairs`에도 없다. 운영 뱅크의 초기 내용물이 곧 이 세트의 임베딩이다.
_Avoid_: "기존 학습 데이터"(범위 모호 — 큐레이션에서 흘러든 것까지 포함하는 것으로 오해)

**회귀 평가셋 (Regression eval set)**:
부트스트랩 세트 전표에 대해 동결한 정답 시퀀스 — 전표마다 `(품목 순번, 정식 라벨, 공급가)`만 담고 행 위치·크롭 경계는 담지 않는다. 파이프라인이 어떻게 바뀌어도 유효하도록 **의도적으로 파이프라인 산출물에 무관한 형태**로 둔다. 변경 전후를 같은 자로 재기 위한 것이므로 사람이 계속 갱신하지 않는다.
_Avoid_: "검증셋"(재학습 중 조기종료에 쓰는 val 분할과 혼동), "테스트셋", "학습 후보 쌍"(이쪽은 계속 늘고 사람이 갱신한다)

**큐레이션 (Curation)**:
재학습 직전, 누적된 학습 후보 쌍을 사람이 검토해 배제·수정·승인하는 단계. 학습 후보가 의도대로 정리됐는지 확인해 정답지의 품질을 보장한다. confirm 시점의 1차 검수와 구분되는 **2차 관문**이다.
_Avoid_: "검수"(confirm 시점 1차 검수와 혼동), "완전 자동화"(로드맵 목표 아님 — 사람 개입은 영구 유지, 재학습으로 배제·수정 비율만 낮춘다. 근거: ADR 0005)

**배제 (Exclusion)**:
학습 후보 쌍을 재학습 입력에서 빼는 결정. 두 갈래가 있고 **반드시 구분해 센다** — 큐레이션에서 사람이 내린 수동 배제와, 빈 크롭처럼 기계가 판정한 자동 배제. 자동 배제도 쌍 자체는 만들어 표시만 달기 때문에 사람이 되돌릴 수 있고, 배제율이 계속 관측 가능하다.
_Avoid_: "제외"(뱅크 채점의 제외 축과 혼동), "크롭 불량"(여러 배제 사유 중 하나일 뿐)

**배제 사유 (Exclusion reason)**:
쌍이 왜 배제됐는지 — 배제 여부와 별개인 축. 사유를 섞으면 배제율이 파이프라인 개선 신호로서의 의미를 잃으므로 갈라 센다. 현재 채워지는 값은 기계 판정 사유 둘(**빈 크롭** / **승계 실패** = 미결 쌍)뿐이고, **이 축은 기계 전용이다**(근거: ADR 0013) — 사람이 고르는 까닭은 사람 배제 사유가 따로 담을 예정이나, 그 컬럼이 아직 없어(S3 도입 예정) 지금은 사람이 배제해도 사유를 고를 수단이 없어 **비어 있다**. 사유가 비어 있다는 것 자체가 "사람 소유"라는 표식이고 재처리 승계가 그 표식으로 사람의 배제를 알아보므로, 사람이 배제하면 기계 사유는 지워진다(근거: ADR 0006).

**사람 배제 사유 (Human exclusion reason)**:
사람이 학습 후보 쌍을 뺀 까닭 — **크롭 경계 불량**(파이프라인 개선 신호) / **원본에 정답 없음**(타이어처럼 관례상 품목명을 생략하고 수량·단가만 적은 줄). 기계 사유와 **다른 자리에 산다** — 다만 그 자리(`training_pairs.human_exclusion_reason`)는 S3에서 신설 예정인 컬럼이며 현재는 존재하지 않는다(레포 전체 참조 0건). 같은 자리에 쓰면 "기계 사유가 비어 있음 = 사람 소유"라는 표식이 파괴되고, 재처리 승계가 사람의 배제를 자기 판정으로 오인해 두 번의 재처리 뒤 그 쌍을 조용히 학습으로 되돌린다. 크롭 경계 불량은 귀책 단계와 같은 어휘를 쓰지만 층위가 다르다 — 크롭은 행마다 갈리므로 잡 단위로 지목할 수 없다.
_Avoid_: "배제 사유"(기계 사유와 같은 자리로 오해), "크롭 불량"(행이 밀린 것과 경계가 잘린 것은 원인·대책이 다르다)

**제외 축 (Exclusion axis)**:
정확도를 채점할 때 뱅크의 어떤 임베딩을 후보에서 빼고 셀지 정하는 기준(자기 자신만 빼기 / 같은 전표에서 나온 것까지 빼기). 같은 전표의 다른 칸이 답을 알려주는 과대평가를 걷어내기 위한 **측정용 장치**이며, 학습 데이터에서 쌍을 빼는 배제와는 무관하다.
_Avoid_: "배제"(학습쌍 배제와 혼동)

**검수 대상 실패 (Reviewable failure)**:
큐레이션 분석이 사람이 눈으로 볼 가치가 있다고 고른 학습 후보 쌍. 품목축·금액축·정합 장애 세 갈래의 합집합이라 **품목 정확도 지표와 분모가 같지 않다** — 지표는 평가 가능한 품목축만 세고, 이쪽은 그 밖까지 모은 검수 작업물이다. 잡 단위로 뭉친 것(어느 잡의 크롭을 내려받을지 고르는 기준)은 층위가 다르며 지표의 모집단이 아니다 — 지표는 확정된 전 잡의 전 쌍에서 나온다.
_Avoid_: 그냥 "실패"(잡 상태 `failed`·강등과 혼동), "실패 잡"(쌍이 아니라 잡 단위 — 층위가 다르다)

**귀책 단계 (Faulting stage)**:
한 잡에서 처음으로 틀어진 단계를 사람이 지목한 판정(쿼드 / 워프 / 행 오프셋 / 행 분류 / 정상) — S3에서 `job_stage_verdicts.fault_stage`로 신설 예정인 어휘이며 현재는 저장 자리가 없다(레포 전체 참조 0건). 잡마다 **최대 하나**이며 그 뒤 단계는 판정하지 않는다 — 앞이 틀어졌으면 뒤가 제대로 될지는 아직 아무도 모르고, 뒤까지 함께 세면 단계별 건수가 앞단 불량의 그림자로 부풀어 "어느 단계를 고치면 몇 건이 풀리나"를 잃는다. **정상도 판정이다** — 봤는데 멀쩡한 것과 아직 안 본 것은 다르며, 이 구분이 없으면 개선 효과 대신 검수 진척을 재게 된다. 재처리하면 기하가 바뀌므로 판정은 **세대에 못 박아 누적한다**. 같은 잡의 세대를 나란히 놓는 것이 곧 기하 단계 개선을 재는 자다 — 회귀 평가셋이 행 위치·크롭 경계를 의도적으로 담지 않아 그 자가 달리 없다. 아무것도 배제하지 않는다(배제는 여전히 쌍의 축이다).
_Avoid_: "실패 단계"(잡 상태 `failed`·강등과 혼동), "불량 태그"(정상 판정이 지워진다)

**행 증감 (Row delta)**:
확정 시점에 사람이 초안에 없던 행을 몇 개 더했고 초안에 있던 행을 몇 개 버렸는지. **방향을 갈라 센다** — 더한 쪽은 행을 놓친 것이고 버린 쪽은 없는 행을 만든 것이라, 같은 기준선을 정반대로 움직여야 한다. 놓친 행은 크롭이 없어 학습 후보 쌍이 되지 못하므로 쌍의 축으로는 표현할 수 없고, 이 수만이 그 사실을 남긴다. 사람 판정을 대신하지 않는다 — 전표에 없는 항목을 사람이 넣은 경우도 같은 수로 보이므로, **어느 잡을 먼저 열어볼지 고르는 힌트**이지 귀책 단계의 판정이 아니다.
_Avoid_: "행검출 불량"(판정으로 오해 — 사람이 넣은 행도 섞인다), "행 이상"(방향이 지워진다)

**처리 관측 (Processing observation)**:
사진 업로드부터 확정 직전까지 잡에 무슨 일이 있었는지를 운영자가 읽기 전용으로 들여다보는 일. **관문이 아니다** — 검수·큐레이션과 달리 아무 결정도 내리지 않고 파이프라인의 어떤 상태도 바꾸지 않으므로, 여기서 본 것을 고치려면 기존 경로(재처리·큐레이션)로 돌아간다. 기계가 이미 아는 것(강등·실패·미확신·적체·미확정)만 드러내며, 화면상 정상으로 보이는 행 오프셋 오염은 여기서 잡지 않고 큐레이션에 맡긴다.
_Avoid_: "모니터링"(무엇을 어디까지 보는지 모호), "검수"(1차 관문과 혼동), "3차 관문"(결정을 내리지 않으므로 관문이 아니다)
